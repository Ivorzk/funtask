{
  "name": "${name}",
  "main": "./index.js",
  "version": "1.0.0",
  "description": "${description}",
  "keywords": "${keywords}",
  "activationCommands": {
    "funtask-workspace": "${name}:toggle"
  },
  "repository": "${repository}",
  "license": "MIT",
  "engines": {
    "funtask": ">=1.0.0 <2.0.0"
  },
  "dependencies": {}
}
